.center-form
  .center-form-inside
    %p#notice= notice

    %p
      %b Title:
      = @post.title
    %p
      %b Body:
      = @post.body

    = link_to t('common.edit'), edit_post_path(@post)
    \|
    = link_to t('common.back'), posts_path
